[project]
name = "peer-rollback-framework"
authors = ["Stacks Community"]
description = "A decentralized risk management protocol for secure asset tracking"
telemetry = false
requirements = []
[contracts.peer-rollback-tracker]
path = "contracts/peer-rollback-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
